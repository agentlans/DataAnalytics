#ifndef _GENERAL
#define _GENERAL

// General headers for shortcut types
#include <vector>
#include <functional>

typedef std::vector<double> Vec;
typedef std::function<double(const Vec&)> Function;

#endif
